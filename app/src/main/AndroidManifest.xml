<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.XyzStudio.dasinventory">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- Permissions for internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DasInventory">
        <activity
            android:name=".History"
            android:exported="false"
            android:label="@string/title_activity_history"
            android:theme="@style/Theme.DasInventory.NoActionBar" />
        <activity
            android:name=".InvHistory"
            android:exported="false"
            android:label="@string/title_activity_inv_history"
            android:theme="@style/Theme.DasInventory.NoActionBar" /> <!-- <activity -->
        <!-- android:name=".HitungBilling" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".FragBilling" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".BillingItem"
            android:exported="false"
            android:label="@string/title_activity_billing_item"
            android:theme="@style/Theme.DasInventory.NoActionBar" /> <!-- <activity -->
        <!-- android:name=".FormBilling" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_form_billing" -->
        <!-- android:theme="@style/Theme.DasInventory.NoActionBar" /> -->
        <activity
            android:name=".BillingMachine"
            android:exported="false"
            android:label="@string/title_activity_billing_machine"
            android:theme="@style/Theme.DasInventory.NoActionBar" /> <!-- <activity -->
        <!-- android:name=".FragItemInv" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_frag_item_inv" -->
        <!-- android:theme="@style/Theme.DasInventory.NoActionBar" /> -->
        <activity
            android:name=".InventoryItem"
            android:exported="false" />
        <activity
            android:name=".Laporan"
            android:exported="false" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".EditProfile"
            android:exported="false" />
        <activity
            android:name=".FormLaporan"
            android:exported="false" />
        <activity
            android:name=".TeknisiActivity"
            android:exported="true" /> <!-- <activity -->
        <!-- android:name=".FormInventory" -->
        <!-- android:exported="true" /> -->
        <activity
            android:name=".Inventory"
            android:exported="true" />
        <activity
            android:name=".CaptureAct"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme" />
        <activity
            android:name=".Home"
            android:exported="true" />
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>